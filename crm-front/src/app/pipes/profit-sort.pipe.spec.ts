import { ProfitSortPipe } from './profit-sort.pipe';

describe('ProfitSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfitSortPipe();
    expect(pipe).toBeTruthy();
  });
});
